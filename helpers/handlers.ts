// import type { newRaffleData } from "../pages"
// import {LuckyLensMumbai} from '../lib/contracts/address'


// export const handleNewRaffle = async (params: newRaffleData) => {
//   const {profileId, pubId, date, time, now} = params
//   if(!profileId || !pubId) console.error('tried to create new Raffle without params')
//   if((!date || !time) || !now) console.error('tried to create new Raffle without params')



//   console.log("new giveaway with params:", params)

//   return null
// }
//doing this in the main page for now
export {}